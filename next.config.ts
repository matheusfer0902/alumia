/** @type {import('next').NextConfig} */
const nextConfig = {
  //basepath: '/wordpress',
  images: {
    
    domains: ['alumia.local', 'localhost', 'alumialab.com.br'],
  },
};

export default nextConfig;
